# Humiley People & Workplace Portal — runs on the Python standard library only.
# No third-party packages required. This file exists so PaaS builders (Render, etc.)
# detect a Python project and complete the build step cleanly.
