# Dev/CI-only dependencies. The app itself runs on the Python standard library (no runtime deps).
# pytest 9 requires Python >= 3.10. Same split, same reason: a bare floor would break the py3.9
# CI job outright, since this file IS installed there.
pytest>=9.1.1; python_version >= "3.10"
pytest>=7.4,<9; python_version < "3.10"
