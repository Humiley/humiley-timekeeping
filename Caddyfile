# Caddy automatically obtains + renews a Let's Encrypt certificate for this domain
# and reverse-proxies HTTPS traffic to the app container. The domain comes from .env.
{$PORTAL_DOMAIN} {
	# HSTS at the TLS edge — force HTTPS for a year (Caddy does NOT set this by default).
	# Add `preload` only once you're certain every subdomain is HTTPS-only and you submit to
	# the preload list; it is hard to undo.
	header Strict-Transport-Security "max-age=31536000; includeSubDomains"
	reverse_proxy app:8000
}

# Procurement — an app of this portal, running as a SEPARATE compose stack on the same VPS
# (humiley-procurement). This Caddy reaches it over the shared `humiley_net` network by container
# name. Harmless until DNS for procurement.humiley.com resolves + the procurement stack is up
# (Caddy retries the cert on its own; it will 502 until the app answers, without affecting the
# portal). Point procurement.humiley.com's A record at this VPS to activate it.
procurement.humiley.com {
	header Strict-Transport-Security "max-age=31536000; includeSubDomains"
	reverse_proxy humiley_proc_app:3000
}
